# -*- coding: utf-8 -*-

salario_por_hora = float(input("Entre com o valor da hora trabalhada: "))
qtde = float(input("Entre com as horas trabalhadas no mês: "))

valor_total = salario_por_hora * qtde

print("O valor total a receber por mês é: R$", valor_total)